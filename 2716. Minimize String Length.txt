class Solution {
    public int minimizedStringLength(String s) {
        HashSet<Character> stringlength = new HashSet<>();
        for(char c : s.toCharArray())
            stringlength.add(c);
            
        return stringlength.size();
    }
}
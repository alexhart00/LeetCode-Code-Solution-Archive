for (Map.Entry<String, Integer> entry : wordOccurrences.entrySet())
            if (entry.getValue() == 1)
                returnArrayList.add(entry.getKey());class Solution {
    public String[] uncommonFromSentences(String s1, String s2) {
        List<String> returnArrayList = new ArrayList();
        Map<String, Integer> wordOccurrences = new HashMap<>();

        for(String word : s1.split(" "))
            wordOccurrences.put( word, wordOccurrences.getOrDefault(word, 0) + 1);

        for(String word : s2.split(" "))
             wordOccurrences.put( word, wordOccurrences.getOrDefault(word, 0) + 1);

        for (Map.Entry<String, Integer> word : wordOccurrences.entrySet())
            if (word.getValue() == 1)
                returnArrayList.add(word.getKey());

        return returnArrayList.toArray(new String[0]);
    }
}
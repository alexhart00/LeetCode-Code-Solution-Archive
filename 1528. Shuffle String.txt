class Solution {
    public String restoreString(String s, int[] indices) {
        StringBuilder shuffleString = new StringBuilder(s);
        for(int i = 0; i < s.length(); i++)
            shuffleString.setCharAt(indices[i], s.charAt(i));

        return shuffleString.toString();
    }
}
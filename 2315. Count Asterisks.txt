class Solution {
    public int countAsterisks(String s) {
        int count = 0;
        boolean toCount = true;
        
        for(char c : s.toCharArray()){
            if(c == '|')
                toCount = !toCount;
            if(c == '*' && toCount)
                count++;
        }
        
        return count;
    }
}
class Solution {
    public int findMaxK(int[] nums) {
        Set<Integer> numsSet = Arrays.stream(nums).boxed().collect(Collectors.toSet());
        int max = -1;
        for(int num : nums)
            if(numsSet.contains(-num) && num > max)
                max = num;

        return max;
    }
}